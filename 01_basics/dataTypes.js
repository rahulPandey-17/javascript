"use strict" // treat all the js code as newer version

let name = "Rahul Pandey";  // string
let age = 24;  // number
let isLoggedIn = true;  // boolean

/*
 * null => standalone value
 * undefined => value is not assigned yet
 * symbol => to identify uniqueness
 * object => reference type(above all are primitive)
*/

// typeof - tells us the data-type of a variable

console.log(typeof null); // it will show null as a object
console.log(typeof undefined); // it will show undefined as undefined